# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Auth / security ---
# passlib is unmaintained and broken with bcrypt>=4.1 (crashes on hash());
# using the `bcrypt` package directly instead (see app/core/security.py).
bcrypt==5.0.0
pyjwt==2.10.1

# --- Object storage (Supabase) ---
# 2.11.0's client only accepted legacy JWT-format keys and rejected
# Supabase's newer sb_secret_/sb_publishable_ key format outright.
supabase==2.31.0


# --- Resume parsing ---
# Docs list "spaCy/regex" for entity extraction (either, not both) — the
# regex + section-heuristic parser in document_ai/parser.py tested well
# on real-shaped sample CVs and is deterministic/easy to unit test, so
# spaCy (a much heavier dependency) wasn't added. Worth revisiting if
# accuracy on messier real CVs turns out to need it.
pymupdf==1.25.1
pdfplumber==0.11.4
python-docx==1.1.2

# --- Generative AI ---
# Provider is Gemini for this project (see app/modules/matching_engine/llm_assist.py
# for the swappable interface — add langchain-openai here too if switching providers).
langchain==0.3.13
langchain-google-genai==2.0.7

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Utilities ---
python-dotenv==1.0.1
